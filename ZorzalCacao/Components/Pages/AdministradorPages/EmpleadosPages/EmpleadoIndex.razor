@page "/ListaEmpleados/Index"
@inject IUserRoleService userRoleService
@inject NavigationManager nav
@rendermode InteractiveServer

<PageTitle>Consulta de Empleados</PageTitle>

<div class="container-fluid px-4 mt-4 cacao-background">

    <!-- ✅ ENCABEZADO CACAO -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h3 class="fw-bold text-brown mb-0">
                <i class="bi bi-people me-1"></i> Empleados
            </h3>
            <small class="text-muted">Consulta general de empleados del sistema</small>
        </div>
    </div>

    <!-- ✅ TARJETA PRINCIPAL -->
    <div class="card shadow border-0 rounded-4 bg-cacao-light">

        <!-- ✅ FILTROS -->
        <div class="card-body border-bottom bg-cacao rounded-top-4">
            <div class="row align-items-end g-3">

                <div class="col-12 col-md-4">
                    <label class="form-label fw-semibold text-white">Filtrar por</label>
                    <InputSelect class="form-select" @bind-Value="Filtro">
                        <option value="" disabled selected>Seleccione una opción</option>
                        <option value="Id">Id</option>
                        <option value="UserName">Usuario</option>
                        <option value="Email">Email</option>
                    </InputSelect>
                </div>

                <div class="col-12 col-md-6">
                    <label class="form-label fw-semibold text-white">Búsqueda</label>
                    <div class="input-group">
                        <input class="form-control" @bind="ValorFiltro" placeholder="Buscar empleado..." />
                        <button class="btn btn-outline-light bi bi-search" @onclick="Buscar"></button>
                    </div>
                </div>

            </div>
        </div>

        <!-- ✅ TABLA CACAO -->
        <div class="table-responsive">
            <table class="table align-middle mb-0 text-center">

                <thead style="background:#5a3a1b; color:white;">
                    <tr>
                        <th>Usuario</th>
                        <th>Email</th>
                        <th>Acciones</th>
                    </tr>
                </thead>

                <tbody class="text-center bg-white">

                    @if (ListaEmpleados == null || !ListaEmpleados.Any())
                    {
                        <tr>
                            <td colspan="3" class="py-4 text-muted">
                                <i class="bi bi-info-circle"></i> No se han encontrado empleados.
                            </td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var emp in ListaEmpleados)
                        {
                            <tr style="border-bottom:1px solid #e7d8c9;">
                                <td class="fw-semibold">@emp.UserName</td>
                                <td>@emp.Email</td>

                                <!-- ACCIONES -->
                                <td class="d-flex justify-content-center">
                                    <a class="btn btn-sm btn-outline-cacao bi bi-eye"
                                       href="/ListaEmpleados/View/@emp.Id"
                                       title="Ver"></a>

                                    <a class="btn btn-sm btn-outline-warning bi bi-pencil-square ms-1"
                                       href="/ListaEmpleados/Edit/@emp.Id"
                                       title="Editar"></a>
                                </td>
                            </tr>
                        }
                    }
                </tbody>

            </table>
        </div>

        <!-- FOOTER -->
        <div class="card-footer">
            <label class="badge bg-dark fs-6 m-0"> Total: @ListaEmpleados?.Count </label>
        </div>

    </div>
</div>

@code {
    public string Filtro { get; set; } = string.Empty;

    public string ValorFiltro { get; set; } = string.Empty;

    public List<ApplicationUser> ListaEmpleados { get; set; } = new();

    protected override async Task OnInitializedAsync()
    {
        await Buscar();
    }

    public async Task Buscar()
    {
        var lista = await userRoleService.GetEmpleadosAsync();

        if (!string.IsNullOrWhiteSpace(ValorFiltro))
        {
            // Filtrar por UserName
            if (Filtro == "UserName")
            {
                lista = lista.Where(u =>
                    u.UserName.Contains(ValorFiltro, StringComparison.OrdinalIgnoreCase)).ToList();
            }

            // Filtrar por Email
            else if (Filtro == "Email")
            {
                lista = lista.Where(u =>
                    (u.Email ?? "").Contains(ValorFiltro, StringComparison.OrdinalIgnoreCase)).ToList();
            }
        }

        ListaEmpleados = lista;
    }
}

